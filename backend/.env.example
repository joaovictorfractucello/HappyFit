# Banco de dados (Neon)
DATABASE_URL="<sua URL pooled, a com -pooler no host>"
DIRECT_URL="<sua URL direta, a sem -pooler>"

# Servidor
PORT=3333

JWT_SECRET="<string aleatória longa; gere com: openssl rand -base64 48>"
JWT_EXPIRES_IN="7d"
